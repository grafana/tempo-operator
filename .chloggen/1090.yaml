# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. tempostack, tempomonolithic, github action)
component: tempo-operator

# A brief description of the change. Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Update to controller-runtime to v0.19.1, controller-gen to v0.16.5, and kubernetes dependencies to v0.31.0.

# One or more tracking issues related to the change
issues: [1090]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  Note that controller-runtime v0.19.1 incldued some breaking changes. Those were addressed as part of this change.
  Users of the operator should not be affected since the operator is handling the changes internally.
