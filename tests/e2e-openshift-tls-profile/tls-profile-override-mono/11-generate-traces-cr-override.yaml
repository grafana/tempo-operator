apiVersion: batch/v1
kind: Job
metadata:
  name: generate-traces-grpc-override
  namespace: chainsaw-tls-profile-mono
spec:
  template:
    spec:
      containers:
      - name: telemetrygen
        image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:v0.92.0
        args:
        - traces
        - --otlp-endpoint=dev-collector:4317
        - --service=grpc-override
        - --otlp-insecure
        - --traces=10
      restartPolicy: Never
