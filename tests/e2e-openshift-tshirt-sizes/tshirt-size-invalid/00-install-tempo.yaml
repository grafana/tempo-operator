apiVersion: tempo.grafana.com/v1alpha1
kind: TempoStack
metadata:
  name: invalid-size
spec:
  size: 1x.invalid
  storage:
    secret:
      name: minio-test
      type: s3
  storageSize: 200M
  tenants:
    mode: openshift
    authentication:
      - tenantName: dev
        tenantId: "1610b0c3-c509-4592-a256-a1871353dbfa"
  template:
    gateway:
      enabled: true
    queryFrontend:
      jaegerQuery:
        enabled: true
