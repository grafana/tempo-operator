apiVersion: tempo.grafana.com/v1alpha1
kind: TempoMonolithic
metadata:
  name: mono-route
  namespace: chainsaw-mono-route
spec:
  timeout: 2m
  jaegerui:
    enabled: true
    route:
      enabled: true