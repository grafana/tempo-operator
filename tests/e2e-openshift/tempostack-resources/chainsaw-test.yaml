apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: tempostack-resources
spec:
  description: Test global and per-component resources allocation in TempoStack.
  steps:
  - name: Install Minio
    try:
    - apply:
        file: 00-install-storage.yaml
    - assert:
        file: 00-install-storage-assert.yaml
  - name: Create TempoStack with resources assigned globally
    try:
    - apply:
        file: 01-install-tempostack.yaml
    - assert:
        timeout: 5m
        file: 01-install-tempostack-assert.yaml
  - name: Update TempoStack with per-component resource allocation
    try:
    - apply:
        file: 03-update-tempostack.yaml
    - assert:
        timeout: 5m
        file: 03-update-tempostack-assert.yaml
 
