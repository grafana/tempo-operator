apiVersion: tempo.grafana.com/v1alpha1
kind: Microservices
metadata:
  name: simplest
spec:
  storage:
    secret: minio-test
  template:
    queryFrontend:
      jaegerQuery:
        enabled: true
  retention:
    perTenant: {}
  limits:
    perTenant: {}
