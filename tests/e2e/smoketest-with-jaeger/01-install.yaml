apiVersion: tempo.grafana.com/v1alpha1
kind: Microservices
metadata:
  name: simplest
spec:
  storage:
    secret: test
  template:
    queryFrontend:
      jaegerQuery:
# FIXME remember to reset this to falso before checkin
        enabled: true
  retention:
    perTenant: {}
  limits:
    perTenant: {}
