apiVersion: tempo.grafana.com/v1alpha1
kind: Microservices
metadata:
  name: simplest
spec:
  storage:
    # This is the minio secret
    secret: test
  template:
    queryFrontend:
      jaegerQuery:
        enabled: true
  retention:
    perTenant: {}
  limits:
    perTenant: {}
