apiVersion: tempo.grafana.com/v1alpha1
kind: Microservices
metadata:
  name: simplest
spec:
  storage:
    secret: test
    # FIXME if we're not deploying Jaeger do we need template?
  template:
    queryFrontend:
      jaegerQuery:
        enabled: false
  retention:
    perTenant: {}
  limits:
    perTenant: {}
